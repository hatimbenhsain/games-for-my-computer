using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WordScript : MonoBehaviour
{
    [Tooltip("Please have the word in lower caps except for important capitals (e.g.: the Sun)!")]
    public string word;
    void Start()
    {
//        FindObjectOfType<ComplimentManager>().AddWord(word);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
